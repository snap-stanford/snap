GLIB Merge Notes
Jure, Aug 24, 2012

** Tadej : change style of code: tabs to spaces, braces, etc.

** DT.H
-- TMem -- check after Viktor [Marko]
-- TChA+= operator (change the order of function declarations) [Blaz]
-- merge TStrPool64 into the official branch [Blaz]
-- keep Marko's TSStrPool (it was commented out IJS version) [Blaz]
-- reformat Tadej's code to follow GLib style (IsPrefix, IsSuffix) [Tadej]
-- Hash function: ask Janez to add TPair, TTriple, TTupple, TUInt64 hash functions [Janez]

** DT.CPP 
-- TMem::Resize revert back to SNAP version (without the redundant if and while) [Marko]
-- TMem::AddBf: update the style of the new code (which uses Reserve, memcpy) [Marko]
-- reformat Tadej's code to follow the GLib style (IsPrefix, IsSuffix) [Tadej]
-- TStrPool -- change function parameters to const reference [Rok]
	DONE -- TStrPool -- change IAssert to IAssertR [Rok]
-- TStrPool64 indent 2 spaces [Blaz]
-- TStr::Left, Right, Slice, Mid, operator() -- clean these functions [Marko]

** DS.H
-- TPair hash function [Janez]
-- TTriple hash function [Janez]
-- Ttuple hash function [Janez]
	DONE -- TCmpKeyDatByDat use IJS version (TKeyDat as parameter not TPair) [Rok]
	DONE -- GetPivotValNCmp remove ;; [Rok]
-- What's the difference between TVec:Diff and TVec::Minus [Blaz]
	DONE -- TVecPool::Resize : comment out printf [Rok]
	DONE -- TVecPool::CompactPool : comment out the printf statement [Rok]

	** HASH.H
	DONE -- in hash.h add THKeyDat (from IJS GLib) [Rok]
	DONE -- THashKeyDatI::IsEnd -- keep the "const" version, remove the non-const version [Rok]

	** GNUPLOT.*
	DONE -- keep SNAP version [Rok]

** TM.*
-- TSecTm::SubSecs (Sub...) check for underflows -- time cannot get negative [Blaz]
-- TTm:IsTimeDef -- check this function for consistency [Blaz&Marko]
	DONE -- Keep the IJS version [Rok]

** OS.H
-- put GUID into a separate file [Blaz]

** SS.H
-- TSs::LoadTxt -- compress the series of ifs (define a separator character and separator type) [Marko]
	DONE -- TSsParser:Next -- remove old version [Rok]

	** ZIPFL.H
	DONE -- keep SNAP version (Tadej can write a new TZipIn class) [Rok]

** JSON.H
-- keep IJS version [Blaz]

	** OS.H
	DONE -- TSysTm::GetTmFromMSecs -- check these functions. Jure put FailR and indicated a bug there. Find Linux equvivalent. [Rok]
	DONE -- TSysProc::Sleep -- just commented out 3 lines [Rok]

** BASE.H
-- move console.h to Misc folder [Blaz]
-- move app.h to Misc folder [Blaz]
-- move pp.h to Misc folder [Blaz]
-- move exp.h to Misc folder [Blaz]
-- more macro.h to Misc folder [Blaz]

	** Style guide:
	DONE -- for printf, TStr:Fmt for 64bit ints use %s and TUInt64::GetStr(), due to cross plantform compatibility [Rok]
	DONE -- functions should not return TSize (size_t). Functions should return fixed size arguments (int32, int64) [Rok]

