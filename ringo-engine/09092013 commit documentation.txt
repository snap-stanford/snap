Changes - commit f573249e22eec43733de0eb49223a6aa5925c640 (09/09/2013):
----------------------------------------------------------------------
Predicate.h:
    # clean-up of deprecated code
    # added default constructor for TAtomicPredicate

TTable (Table.h, Table.cpp):
    # re-organized and added documentation to Table.h
    # clean-up of deprecated code
    # added uniqueness verification, using a hash table, when adding a new string value to the table
    # added function TTable::AddIdColumn for adding explicit identifiers for the rows
    # added an option to Select methods - return in a vector the physical ids of the rows that satisfy the predicate
      instead of removing the rows that don't satisfy it.
    # added an option to Order method - reset rank by most significant rank-by column
    # fixed bug in TTable::GetNId
    # Adding node attributes: Allow aggregation of attribute values per node (i.e. resolve conflict when the same node gets 
      contradicting attribute values in different table rows)
    # changes in IsNextK: grouping is now optional; added rank column
    # added function PrintSize for debug printing of some of the object field sizes
    # added function AddTable to add all the rows of a table with same schema
